import { MyMouseOverDirective } from './my-mouse-over.directive';

describe('MyMouseOverDirective', () => {
  it('should create an instance', () => {
    const directive = new MyMouseOverDirective();
    expect(directive).toBeTruthy();
  });
});
